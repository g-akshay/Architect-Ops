# Knowledge Graph — [Project Name]

> Auto-generated by Architect-Ops `/context-builder`  
> Last updated: [date]  
> Human edits welcome — this is a living document.

---

## Business Concepts

<!-- Add one entry per major business domain concept -->

### [Concept Name, e.g. "User"]
- **Owner module:** `src/modules/users/`
- **Dependents:** `src/modules/orders/`, `src/middleware/auth.ts`
- **Data shape:** `{ id: string, email: string, role: UserRole, createdAt: Date }`
- **Key rules:**
  - Users are always authenticated before reaching service layer
  - User deletion is soft-delete only (never hard delete)
- **Notes:** The `role` field controls feature flags — see `src/config/permissions.ts`

---

## Module Map

| Module | Purpose | Owns | Depends On |
|--------|---------|------|------------|
| `src/modules/users/` | User lifecycle management | User, UserProfile | Auth, Database |
| `src/lib/api-client.ts` | Centralized HTTP abstraction | HTTP conventions | fetch, retry logic |
| *(add your modules)* | | | |

---

## Data Flow

### [Feature Name, e.g. "User Registration"]

1. `POST /auth/register` (Controller) receives email + password
2. `AuthService.register()` validates and hashes password
3. `UserRepository.create()` persists to database
4. `EmailService.sendVerification()` dispatches welcome email
5. Controller returns `{ userId, token }`

---

## External Dependencies

| Package | Version | Used For | Owner Module | Notes |
|---------|---------|---------|-------------|-------|
| *(add packages)* | | | | |

---

## Tribal Knowledge

> Things that are true but not obvious from reading the code.

- **[Topic]:** [Explanation]
- **[Topic]:** [Edge case or historical decision]

---

## Architectural Decisions Log

| Date | Decision | Reason | Alternatives Rejected |
|------|----------|--------|----------------------|
| [date] | [decision] | [reason] | [alternatives] |

---

*This file is maintained by Architect-Ops. Run `/context-builder` to refresh it.*
