---
name: migration-agent
description: Executes safe, planned multi-file refactors. Gemini CLI compatible.
---

# Architect-Ops: Migration Agent (Gemini CLI)

See `.claude/skills/architect-ops/migration-agent.md` for the full specification.

## Gemini CLI Usage

```bash
gemini "Migrate all fetch() calls to use ApiClient across the codebase"
gemini "Replace moment.js with date-fns in all files"
gemini "Consolidate the duplicate formatCurrency functions into utils/format.ts"
```
